# AI 原生量化工作台 · 架构与产品图

> 目标：把「因子发现 → 检验 → 选股 → 组合 → 回测 → 归因 → 迭代」这条链，从**人驱动**变成
> **智能体驱动 + 人定目标与红线**。核心资产是两样：**agent runtime** 与**量化基础设施**（护城河）。

---

## 一、系统架构（分层 + 接缝）

```
              ┌──────── 人：定目标 / 风险预算 / 红线 / 批准因子升格 ────────┐
              └───────────────────────────┬──────────────────────────────┘
                                          │
┌──────────── ① Agent Runtime：DSH ───────┴───────────────────────────────────────┐
│ 角色(skills/plugins)              长任务驱动            记忆                      │
│ 假设官·实现官·组合官·归因官        cron → goal/ralph    因果记忆(含被杀原因)      │
└──────┬───────────────┬──────────────────┬───────────────────┬──────────────────┘
       │提议(带机制)   │因子代码          │已准入因子         │实盘成交/持仓
       ▼               ▼                  ▼                   ▲
┌──────────────── ② 量化基础设施（框架无关 Python 包，护城河）────────────────────┐
│                                                                                │
│  ┌───────────────┐   ┌──────────────────────────────────┐   ┌───────────────┐  │
│  │ artifacts.db  │◄──│  gate.py  【唯一准入权 · 无 LLM】 │──►│ registry      │  │
│  │ 假设/因子/评估│   │  净化 OOS IC · 多重检验校正      │   │ 因子动物园    │  │
│  │ 准入/组合/清单│   │  ★净成本★ · 换手约束             │   │ 候选/在用/退役│  │
│  │ 归因(版本化)  │   │  → 交付三个数: 净超额/换手/t     │   │ +退役原因     │  │
│  └───────────────┘   └──────────────────────────────────┘   └───────────────┘  │
│          ▲                            ▲                                       │
│          │                            │ factor_code                           │
│  ┌───────┴────────────────────────────┴─────────────────────────────────────┐  │
│  │ 数据/因子底座（hub MCP：111 工具）                                        │  │
│  │  cn_data 5206×4302(已修复) · dfactor(Redis 5191) · FactorEngine(64)       │  │
│  │  ml_* · kronos · causal: event_study / causal_impact / refute / dml_cate  │  │
│  └──────────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────────────┘
       │ 已准入因子（契约：dfactor:{symbol}）        ▲ fills / positions
       ▼                                            │
┌──────────────── ③ 执行/记账台：athena（原样保留）───────────────────────────────┐
│ 读 dfactor → WarRoom；持仓与风控(trader-01 三层)；监控；下单清单由人执行          │
└────────────────────────────────────────────────────────────────────────────────┘

┌── ④ 生成引擎（可选，只能挂在闸门**之后**）：RD-Agent（qlib 闭环）───────────────┐
│ 允许：投喂假设/因子表达式      不允许：自审、自准入                              │
└────────────────────────────────────────────────────────────────────────────────┘
```

**三条不可破的接缝**（定了这三条，runtime 换谁都不影响整套）：
1. `dfactor:{symbol}` —— **已准入因子**的唯一交付格式（athena 已经在读，现成）
2. `gate.evaluate(factor_code, universe, window, cost_model) -> verdict` —— **唯一准入入口**，确定性、**无 LLM**
3. `fills / positions` —— **唯一**实盘回吐（供归因与记忆），研究层不回写交易层

---

## 二、产品图（人视角：一天 / 一周 / 三个动作）

```
【每日 3 分钟】
  08:30 ⚙️ 自动  数据更新 → 因子刷新 → 闸门巡检（在用因子 IC 是否衰减）
  14:35 📋 推送  下单清单（代码/方向/金额/股数/限价/理由） ← 人只负责照着点
  15:10 ⚙️ 自动  回填成交与结果 → 写因果记忆

【每周 30 分钟】
  周日 📊 周报
      · 分层：底仓 / 机动 / 做T 各自收益、换手、★成本占比★
      · 归因：CAR 拆出 市场 / 板块 / 个股 alpha
      · 因子：在用因子净 IC 与上月对比 → 衰减者自动降权
      · 纪律：是否越红线（单票上限/追高/未执行）
      · 结论：加码或砍掉哪一层（判据写死，不允许"再等等"）

【人在环上只有三个动作】
  ① 批准/否决「新因子升格入池」——依据是闸门给的数字，不是感觉
  ② 改风险预算与红线
  ③ 执行清单（手动下单）
```

---

## 三、因子的生命周期（闸门画在正中间）

```
假设(带机制) → 实现(面板因子) → [闸门·确定性] → 准入 → 入池 → 小仓上线 → 归因 → 升格/退役
                                        │
                                        └── 否决 → 连**原因**写进因果记忆
                                                    （防止智能体反复提同一类垃圾）
```

---

## 四、现状 vs 缺什么

| 层 | 现状 | 缺口 |
|---|---|---|
| ① Runtime | DSH 已有工具/记忆/subagent/插件 | 缺**常驻驱动**（cron 踢 + 产物落库） |
| ② 底座 | ✅ 数据链已修（dfactor 0→5191、截面 2→5206）· 111 工具 · 因果 mcp | 缺**行业映射**（板块约束/中性化） |
| ② 闸门 | ✅ 今晚建成：多窗口 OOS IC（K=74、自洽性验证）+ 成本/换手测试 | **要产品化**成一个入口 |
| ② 产物库 | 🟡 scanner 里有雏形（candidate_pool） | 缺统一 artifacts（假设/因子/评估/准入/归因） |
| ③ 执行台 | ✅ athena 实盘持仓/风控/监控 | 只加一条 fills 回吐契约 |
| ④ 生成器 | ✅ RD-Agent 本地已配 | 需强制"只投喂、不自审" |

---

## 五、验收标准（工作台自己必须先过这一关）

1. **闸门可信**：对已知的 4 个信号（ML / kronos / α倾斜 / composite）能复现今晚的判决
   （全部否决），且**每个否决都带净超额、换手、t 值三个数**。
2. **产物可复现**：任一准入决定都能从 artifacts 里重放出当时的因子代码、数据区间、成本假设。
3. **记忆生效**：被否决的假设类型，在后续 N 轮里**不再重复提**（可量化）。
4. **端到端一条**：假设官提 5 个带机制假设 → 实现官写成因子 → 闸门逐条杀/留 → 报告。

---

## 六、明确不做（同样重要）

- ❌ 不用 LLM 判准入（**这是整套设计的地基**）
- ❌ 不取"排序前 N 等权"（实测尾部毁价值：毛超额 −4.7%/年）
- ❌ 不重写 athena 当研究台（形状/生命周期冲突）
- ❌ 不在 `gate`/`artifacts` 契约稳定前引入新框架（等于把钱花在胶水上）
- ❌ 不追"因子数量"（瓶颈在闸门与成本，不在候选数）
